use crate::board::board::{Board, Move};

pub struct Solver {}

impl Solver {
    pub fn from_board(board: Board) -> Self {
        unimplemented!();
    }

    pub fn get_next_moves(&mut self) -> Vec<Move> {
        unimplemented!();
    }
}